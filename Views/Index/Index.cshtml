@model IEnumerable<ISurveyModel>
<ul>
    @foreach (ISurveyModel survey in Model)
    {
        <li>
            <a href="~/survey/@survey.Id">
                <h2>@survey.Title</h2>
                <p>@survey.Author</p>
            </a>
        </li>
    }
</ul>